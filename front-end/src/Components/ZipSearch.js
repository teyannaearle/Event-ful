import React from "react";

function ZipSearch({category}) {
  return (
    <div>
      <h1>
        Input zip code above to search for {category} in your area.
      </h1>
    </div>
  );
}

export default ZipSearch;
